// Todo: Reorganize the names here, use something more generic instead of SubCommandsEnum...

export enum SubCommandsEnum {
    Make = 'make',
    Remove = 'remove',
    Document = 'document',
    Serve = 'serve',
    Test = 'test',
}

export enum MakeAliasesEnum {
    Mk = 'mk'
}

export enum RemoveAliasesEnum {
    Rm = 'rm'
}

export enum DocumentAliasesEnum {
    Doc = 'doc'
}
