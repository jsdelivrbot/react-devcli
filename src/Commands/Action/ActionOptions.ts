export const name = 'action';

export const description = 'Action description';

export const flags = {
    component: {
        alias: 'c',
        demand: false,
        describe: 'Create component alongside action file',
    }
};
